# GitHub OAuth Configuration
VITE_GITHUB_CLIENT_ID=your_github_client_id_here

# Backend Environment Variables (not accessible in frontend)
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Self Identity Verification Configuration
VITE_SELF_APP_NAME=TrustHire-Identity-Verification
VITE_SELF_SCOPE=trusthire-identity-verification
VITE_SELF_ENDPOINT=https://your-backend-endpoint.com/verify

# ENS Records Configuration (for setEnsRecords script)
INFURA_API_KEY=your_infura_api_key_here
PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE
PUBLIC_RESOLVER_ADDRESS=0x123456...
ENS_DOMAIN_NAME=yourname.eth
VERCEL_URL=https://yourapp.vercel.app

# Note: 
# - Variables prefixed with VITE_ are accessible in the frontend
# - Variables without VITE_ prefix are only available in the backend/build process
# - Never expose sensitive data like client secrets in VITE_ variables
# - For Self app: You need to set up a backend endpoint that handles the verification
# - The scope should be unique to your application and consistent with your circuit configuration